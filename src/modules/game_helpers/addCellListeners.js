/* import { gameboardFactory } from "../gameboardFactory";
const addCellListeners = (rightBoardEle, enemyGameboard) => {
/*     p1.board.board[i].forEach((e, j) => {
        let cell = document.createElement("div");
        cell.classList.add("cell-p1");
        cell.setAttribute("id", `p1-row${i}-cell${j}`);
        row.appendChild(cell); */
  
        // event listener for p1 clicks on p2 board
/*        let cells = rightBoardEle.querySelectorAll('.cell')
        cells.forEach((cell) => cell.addEventListener("click", (e) => {
            let event = e
            let target = event.target 
            let coord = e.target.id
          if (!player1.isTurn) return;
          enemyGameboard.receiveAttack(coord);
        }));
     });
}
export { addCellListeners }  */